[package]
name = "mm-adapter-basler"
version = "0.1.0"
edition = "2021"

[features]
basler = ["dep:pylon-cxx"]

[dependencies]
micromanager = { path = "../../micromanager" }
pylon-cxx = { version = "0.4", optional = true }
