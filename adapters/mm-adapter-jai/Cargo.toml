[package]
name = "mm-adapter-jai"
version = "0.1.0"
edition = "2021"

[features]
jai = []

[dependencies]
micromanager = { path = "../../micromanager" }

[build-dependencies]
cc = { version = "1", features = ["parallel"] }
